# Copy to .env.local for local dev, or set these as App Settings in Azure.
# Client-visible (baked into the browser bundle at build time)
DATABASE_URL="postgresql://sybadmin:bab8e786b1f93fecbf892a5e55dc2d41@syb-associates-db.postgres.database.azure.com:5432/postgres?sslmode=require"

# Azure runtime
PORT=8080
DEPLOY_TARGET=azure
